{
  "name": "eleventy-site",
  "private": true,
  "version": "1.0.0",
  "author": "Your Name",
  "license": "MIT",
  "description": "Simple eleventy starter site",
  "scripts": {
    "postcss:build": "postcss src/css/taiwind.css -o _site/style.css",
    "serve": "rm -rf _site && npm run postcss:build && npx @11ty/eleventy --serve",
    "build": "rm -rf _site && npx @11ty/eleventy"
  },
  "devDependencies": {
    "@tailwindcss/forms": "^0.2.1",
    "autoprefixer": "^10.0.3",
    "html-minifier": "^4.0.0",
    "postcss": "^8.1.10",
    "postcss-cli": "^8.3.0"
  },
  "dependencies": {
    "alpinejs": "^2.7.3",
    "tailwindcss": "^2.0.1"
  }
}
